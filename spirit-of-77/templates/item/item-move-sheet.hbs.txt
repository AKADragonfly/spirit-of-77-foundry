<form class="{{cssClass}}" autocomplete="off">
  <header class="sheet-header">
    <img src="{{item.img}}" data-edit="img" title="{{item.name}}" height="64" width="64"/>
    <div class="header-fields">
      <h1 class="charname"><input name="name" type="text" value="{{item.name}}" placeholder="Move Name"/></h1>
    </div>
  </header>

  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-tab="description">Description</a>
    <a class="item" data-tab="details">Details</a>
  </nav>

  <section class="sheet-body">
    <div class="tab" data-group="primary" data-tab="description">
      <div class="form-group">
        <label>Description:</label>
        {{editor content=system.description target="system.description" button=true owner=owner editable=editable}}
      </div>
    </div>

    <div class="tab" data-group="primary" data-tab="details">
      <div class="form-group">
        <label>Move Type:</label>
        <select name="system.moveType">
          {{#each moveTypeOptions}}
          <option value="{{key}}" {{#if selected}}selected{{/if}}>{{label}}</option>
          {{/each}}
        </select>
      </div>

      <div class="form-group">
        <label>Stat:</label>
        <select name="system.stat">
          {{#each statOptions}}
          <option value="{{key}}" {{#if selected}}selected{{/if}}>{{label}}</option>
          {{/each}}
        </select>
      </div>

      <div class="form-group">
        <label>Roll Formula:</label>
        <input name="system.rollFormula" type="text" value="{{system.rollFormula}}" placeholder="2d6"/>
      </div>
      
      <div class="form-group">
        <label>Move Modifier:</label>
        <input name="system.modifier" type="text" value="{{system.modifier}}" placeholder="+1, +2, etc."/>
        <small>Optional static bonus/penalty for this move</small>
      </div>

      <div class="results-section">
        <h3>Results</h3>
        
        <div class="result-group">
          <label>Success ({{system.success.value}}+):</label>
          <input name="system.success.value" type="number" value="{{system.success.value}}" min="1" max="12"/>
          <textarea name="system.success.text" placeholder="What happens on a success...">{{system.success.text}}</textarea>
        </div>

        <div class="result-group">
          <label>Partial Success ({{system.partial.value}}-{{subtract system.success.value 1}}):</label>
          <input name="system.partial.value" type="number" value="{{system.partial.value}}" min="1" max="12"/>
          <textarea name="system.partial.text" placeholder="What happens on a partial success...">{{system.partial.text}}</textarea>
        </div>

        <div class="result-group">
          <label>Failure ({{subtract system.partial.value 1}} or less):</label>
          <textarea name="system.failure.text" placeholder="What happens on a failure...">{{system.failure.text}}</textarea>
        </div>
      </div>
    </div>
  </section>
</form>